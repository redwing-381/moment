# Environment variables for Moment - AI Risk Gatekeeper
# Copy this to .env and fill in your actual values

# ============================================
# Confluent Cloud Kafka Configuration
# ============================================
# Get these from: https://confluent.cloud → Cluster → API Keys
KAFKA_BOOTSTRAP_SERVERS=your-cluster.region.gcp.confluent.cloud:9092
KAFKA_SECURITY_PROTOCOL=SASL_SSL
KAFKA_SASL_MECHANISM=PLAIN
KAFKA_SASL_USERNAME=your-api-key
KAFKA_SASL_PASSWORD=your-api-secret

# ============================================
# Confluent Schema Registry Configuration
# ============================================
# Get URL from: Confluent Cloud → Environment → Schema Registry → API endpoint
# Get API key from: Schema Registry → API credentials → Create key
SCHEMA_REGISTRY_URL=https://psrc-xxxxx.region.gcp.confluent.cloud
SCHEMA_REGISTRY_API_KEY=your-schema-registry-api-key
SCHEMA_REGISTRY_API_SECRET=your-schema-registry-api-secret

# ============================================
# Confluent ksqlDB Configuration
# ============================================
# Get endpoint from: Confluent Cloud → ksqlDB → Cluster settings → Endpoint
# Get API key from: ksqlDB cluster → API keys → Create key
KSQLDB_ENDPOINT=https://pksqlc-xxxxx.region.gcp.confluent.cloud:443
KSQLDB_API_KEY=your-ksqldb-api-key
KSQLDB_API_SECRET=your-ksqldb-api-secret

# ============================================
# Confluent Cloud Metrics API Configuration
# ============================================
# Get API key from: Confluent Cloud → Settings → API keys → Cloud resource management
# Get Cluster ID from: Cluster → Cluster settings → General → Cluster ID (starts with lkc-)
# Get Environment ID from: URL when viewing environment (starts with env-)
CONFLUENT_CLOUD_API_KEY=your-cloud-api-key
CONFLUENT_CLOUD_API_SECRET=your-cloud-api-secret
CONFLUENT_CLUSTER_ID=lkc-xxxxx
CONFLUENT_ENVIRONMENT_ID=env-xxxxx

# ============================================
# Google Cloud Vertex AI Configuration
# ============================================
# Get these from: https://console.cloud.google.com
VERTEX_AI_PROJECT_ID=your-gcp-project-id
VERTEX_AI_LOCATION=us-central1
VERTEX_AI_MODEL_NAME=gemini-1.5-pro

# For local development only (not needed in Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
